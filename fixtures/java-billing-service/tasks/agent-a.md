# Agent A task: stabilize statement close dates

Customer statements created at the same instant must receive the same close date
in every supported test environment. Diagnose the failing fixture behavior and
make the smallest production change that satisfies that requirement.

Start by following `AGENTS.md`. Once the behavior is understood and resolved,
record a concise reusable Common Knowledge Entry with concrete retrieval cues
and the affected source path. Commit that Entry with the code change for a later
agent.
