# Agent B task: add an unpaid-invoice follow-up decision

Add a small decision that determines whether an unpaid invoice should receive a
follow-up on its close-date boundary. Keep the decision testable and consistent
with the billing service's existing time behavior.

Start by following `AGENTS.md`. If Common Knowledge returns an Entry relevant to
this task or the affected billing sources, read it before designing the change.
