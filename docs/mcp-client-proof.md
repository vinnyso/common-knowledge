# Codex MCP client proof

This document records the bounded manual behavior proof required by AF-02. It
will be completed against the implementation candidate before independent
review. Automated adapter and filesystem coverage remains in `test/mcp.test.mjs`.

## Versions and candidate

- Candidate revision: pending
- Node.js: pending
- npm: pending
- MCP TypeScript SDK: `@modelcontextprotocol/sdk` `1.30.0`
- Codex client: pending
- Model and effort: pending

## Discovery and direct operations

Pending candidate run. Record only the configured root, tool discovery, called
operations, and structured outcomes; do not store a full session transcript.

## Ordinary-task consultation

Pending candidate run. Use a temporary isolated repository and short activation
instructions that tell the agent to consult Common Knowledge before relevant
changes. Give the agent an ordinary engineering task that does not mention CK,
then record whether `search` and any applicable `read` occur before the relevant
implementation decision.

This is a manual client behavior proof, not an automated adapter test or an
effectiveness claim.
