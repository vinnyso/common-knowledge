# Domain documentation

This repository has a single domain context.

Before work begins, read the root `CONTEXT.md` and any relevant decisions in
`docs/adr/`. Use the terminology established in `CONTEXT.md`; surface any
conflict with an existing ADR instead of silently overriding it.

